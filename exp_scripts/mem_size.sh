#!/bin/bash
# We already have the data for Parquet and FFF. So ignore them for fast exps.
# TMPDIR=/mnt/nvme0n1/xinyu/tmp cargo run --example bench_mem --release 2>&1 | tee rg_size_laion.log; send_email laiondone
# TMPDIR=/mnt/nvme0n1/xinyu/tmp cargo run --example bench_mem --release 2>&1 | tee rg_size_core.log; send_email coredone

